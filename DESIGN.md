# Design

<!-- impeccable:design-schema 1 -->

Візуальна система онлайн-курсу «Операційний менеджмент» НУ «Чернігівська політехніка».
Записано з побудованих макетів (`design/mockups/`), а не з намірів. Токени — `design/tokens.css`.

> Макети в `design/mockups/` — історичні прототипи, зроблені для попереднього курсу кафедри
> («протокол загальних зборів»). Вони лишені як свідчення дизайн-системи, але джерело правди —
> реалізація в `src/`: метафора інтерфейсу з того часу змінена на маршрутну карту.

## Напрям: «Протокол загальних зборів»

Курс виглядає як бездоганно набраний корпоративний документ, а не як LMS-сітка карток.
Порядок денний замість «карток курсу», кворум замість прогрес-бара, нумеровані питання замість
плиток тем, тонка лінійка чорнилом, табличні цифри. Бренд університету живе в точних місцях:
синя «шапка документа», орбітальний патерн із брендбуку, шестикутник — лише для рівня і бейджів.

Чому саме так. Аудиторія — бакалаври менеджменту; їхній професійний світ — статути, протоколи,
річні звіти, реєстри. Документна форма робить зміст курсу впізнаваним і водночас дозволяє
apple-подібну стриманість: типографіка веде, простір великий, глибина мінімальна. Головна —
між Persuade і Read, тема — Read, тест і кабінет — Operate, профіль — Operate з теплом.

Напрям обрано за протоколом impeccable (seed `2fce709f`, кандидат 5 із 7 у списку за резонансом:
річний звіт, статут, реєстр акціонерів, схема органів, **протокол зборів**, біржове табло,
лекційна дошка). Челенджери з каталогу зважені на двох осях (упізнаваність аудиторією, ясність
продукту):

| Челенджер | Вердикт | Що взято (дисципліна, не костюм) |
|---|---|---|
| Патентне креслення | конкурентний | схеми як рисунки з нумерованими виносками, один карміновий колір «під розглядом» |
| Стрічка провенансу | конкурентний | статус знання видимий: норма з датою перевірки і посиланням, кейс позначений «навчальний за мотивами» |
| Ikeda datamatics | відхилений | табличні цифри всюди, де числа стоять стовпчиком |
| Монтажний стіл | відхилений | стан — знак (чек, пунктир, порожнє коло), а не колір |
| Seedbed lobes | відхилений | колір на масштабі регіону (уся синя смуга), а не розсипані акценти |
| Коптське шиття | відхилений | структура завжди видима: 12 комірок кворуму, «5 із 12», «Показано 8 із 44» |

## Бренд в інтерфейсі

Джерело — офіційний брендбук університету (2021) і пакет логотипів зі stu.cn.ua.

- Основний синій `#004F8F` — колір «шапки документа» (hero головної), первинних кнопок,
  посилань, активних станів. Сайт університету рендерить його як `#005B9F`; ми йдемо за брендбуком.
- Темно-синій `#002D5D` — основа темної теми (тло `#0B1424` і поверхні — похідні), тіні.
- Лавандовий `#A59FCE` — лише графіка: орбітальний патерн на синьому, заливка шестикутника,
  іконки тренажерів (текстова версія `#5F579B`, 5,6:1).
- Бірюзовий `#00796B` з eln — «вірно / перевірено» (правильна відповідь, дата перевірки норми).
- Патерн бренду — орбітальні кола («коло символізує безперервність розвитку») — авторський SVG
  у куті синьої смуги, ніколи поверх тексту. Шестикутник із сайту університету — форма бейджа
  і рівня, ніде більше.
- Логотип — офіційний PNG з альфою (`design/assets/logo-cpnu-uk.png`, 760 px, і біла версія
  для синього/темного). Векторного українського файла університет не публікує; на Wikimedia
  Commons є `CPNU-logo-en.svg` з англійським підписом (суспільне надбання). Емблему не
  перемальовуємо. Мінімальна висота в шапці — 2,1 rem; у футері — 3,3 rem.

## Токени

### Колір (світла тема; темна — у `tokens.css`)

| Роль | Значення | Використання |
|---|---|---|
| `--bg` | `#FCFCFC` | тло сторінки (як на сайті університету) |
| `--bg-sunken` | `#F3F5F8` | заголовки таблиць, ховер рядків, підсвітка «ви тут» |
| `--surface` | `#FFFFFF` | картки, поля, панелі |
| `--surface-tint` | `#EEF4FA` | блок норми права, «Викладачам», чипи ПРН |
| `--band` / `--band-ink` | `#004F8F` / `#FFF` | синя смуга |
| `--ink` / `--ink-2` / `--ink-3` | `#0F1C2E` / `#445570` / `#5F6F88` | текст 15:1 / 7:1 / 4,77:1 (AA; первісне `#6B7A90` давало лише 4,08:1) |
| `--rule` / `--line` | `#0F1C2E` / `#E3E8EF` | лінійка документа 1 px / роздільники |
| `--accent` | `#004F8F` | дія; ховер `#00437A` |
| `--ok` / `--err` / `--warn` | `#00796B` / `#B42318` / `#8A5A00` | вірно / невірно / увага (завжди зі знаком) |
| `--xp` | `#B8860B` | XP — латунний, не «золото» |
| `--level` | `#5F579B` | назва рівня |
| `--focus` | `#1D7AD8` | кільце фокуса 2 px, offset 3 px |

Темна тема — не інверсія: тло `#0B1424`, поверхні `#121D31`/`#182540`, синя смуга `#0F2A52`,
акцент освітлений до `#3F8FD9` з темним текстом на ньому, посилання `#79B4EC`. Перемикання —
`prefers-color-scheme` (без `[data-theme="light"]`) і `[data-theme="dark"]`; вибір користувача
зберігається в `localStorage` під ключем `om:v1:theme`.

### Типографіка

Одна родина — **Open Sans Variable** (wght 300–800, wdth 75–100; OFL 1.1; self-hosted woff2,
субсети cyrillic-ext, cyrillic, latin-ext, latin — `design/fonts/`). Дисплейний голос — та сама
родина у звуженому накресленні: `font-variation-settings: "wdth" 88` для display, 90 для h1,
92 для h2; текст — wdth 100. Це дає власний характер без другого шрифту і відповідає брендбуку
(основні накреслення — Semibold і Bold).

| Стиль | Розмір / інтерліньяж | Вага, ширина, трекінг |
|---|---|---|
| display | clamp(40, 56) / 1.1 | 700, wdth 88, −0.022em |
| h1 | clamp(32, 40) / 1.12 | 700, wdth 90, −0.012em |
| h2 | 28 / 1.2 | 650, wdth 92 |
| h3 | 21 / 1.3 | 650 |
| lede | 21 / 1.45 | 400, `--ink-2`, 44ch |
| лонгрід | 18 / 1.65 | 400, міра 68ch |
| UI | 17 / 1.5 | 400 |
| small | 15 | — |
| caption | 13 | 650, caps +0.06em лише для заголовків колонок і футера |

Числа — `font-variant-numeric: tabular-nums` усюди, де стоять стовпчиком (`.num`). `text-wrap:
balance` на заголовках, `pretty` на абзацах. Українська типографіка: «лапки», апостроф ’,
тире —, `&nbsp;` після № і ст., числа `1 234,5`.

### Простір, радіуси, тіні, рух

- Простір 4-кроковий: 4, 8, 12, 16, 24, 32, 48, 64, 96, 128. Контейнер 76 rem, читання 46 rem,
  ґаттер `clamp(1rem, 4vw, 2.5rem)`. Над заголовком більше, ніж під ним (секція 64 / 24).
- Радіуси: 6 (контроли), 10 (картки), 16 (панелі), pill — лише чипи, сегментовані перемикачі, тост.
- Тіні зі зміщенням і м'яким розмиттям, забарвлені темно-синім: `--shadow-1` (1 px), `--shadow-2`
  (картка при ховері), `--shadow-3` (поповер, липка панель). Без кольорових «сяйв».
- Рух: `--ease-out: cubic-bezier(.16,1,.3,1)`; 140 ms мікро, 260 ms панелі, 480 ms одна авторська
  мить на сторінку (поповер, розбір відповіді, метр XP). `prefers-reduced-motion` обнуляє
  тривалості й `scroll-behavior`.

## Компоненти

- **Шапка** (`.topbar`): липка, напівпрозора з blur; логотип + назва курсу; навігація (Курс,
  Теми, Тести, Тренажери, Картки); перемикач виду Студент / Викладач (сегментований, pill);
  пошук; тема; чип профілю (шестикутник, рівень, XP). Брейкпоінти: 1360 — ховається підзаголовок,
  1280 — навігація йде в меню, 1024 — перемикач виду в меню/футер, 560 — лише логотип і іконки.
- **Синя смуга** (`.band`): hero з дисплейним заголовком, декою, двома CTA (біла первинна на
  синьому), фактами (4 · 12 · 8 · 120) і панеллю кворуму.
- **Кворум** (`.quorum`): 12 комірок, пройдено — білі, поточна — штрихована, решта — прозорі.
  Підпис «5 із 12 тем». Це єдиний прогрес-індикатор головної.
- **Порядок денний** (`.agenda`, `.module`, `.topic`): 4 колонки модулів, лінійка чорнилом над
  назвою модуля, теми як нумеровані рядки зі знаком стану (чек / play / порожнє коло), часом і
  результатом тесту. 2 колонки ≤1024, 1 — ≤640.
- **Картка теми / тренажера** (`.card.tool`): іконка лавандова, назва, один абзац, формула
  дрібним. Ховер — тінь 2 і зсув −2 px.
- **Лонгрід** (`.read`): міра 68ch, 18/1.65, h2 з номером розділу (`1.1`) — номери несуть
  структуру, тому дозволені. Списки з маркером у `--ink-3`.
- **Схема** (`.figure`, `svg.scheme`): рисунок з `<title>`/`<desc>`, блоки з обведенням 1,25 px,
  стрілки суцільні (повноваження) і пунктирні (звітність), нумеровані виноски в колах, один
  карміновий елемент «під розглядом»; легенда під рисунком. Кольори — з токенів, тому схема
  живе в обох темах.
- **Термін-підказка** (`.term` + `.pop`): кнопка з крапковим підкресленням синім, Popover API
  (`popovertarget`), позиціонування біля кнопки, `aria-describedby`. Поповер — поверхня, тінь 3,
  заголовок терміна, визначення, джерело.
- **Норма права** (`.norm`): тонований блок, іконка терезів, цитата курсивом, рядок джерела з
  посиланням на zakon.rada.gov.ua і бірюзовим «перевірено дд.мм.рррр».
- **Кейс** (`.case`): картка з підписом «навчальний кейс за мотивами», питання, `<details>`
  «Показати розбір».
- **Питання тесту** (`.q-stem`, `.options`, `.opt`): варіанти — кнопки з літерою в колі; після
  вибору `data-answered`: правильний — бірюзовий фон і чек, вибраний неправильний — карміновий
  і хрест, решта тьмяніє до 0,72; під кожним варіантом розкривається пояснення (`.opt-why`).
  Вердикт (`.verdict`) з `aria-live`, рядок норми, «Далі» вмикається лише після відповіді.
  Навігатор (`.cells`) — 15 комірок зі знаком результату; XP-бокс з метром.
- **Тост XP** (`.toast`): чорнильна pill знизу, `role="status"`, 3,2 с, XP латунним.
- **Прогрес / рівень / бейдж**: метр (`.meter`, XP — латунний), сходинки рівнів (`.ladder`,
  `.level`) з шестикутником і порогом XP, поточний підсвічений `--bg-sunken`; бейдж — шестикутник
  із лінійною піктограмою, назва, умова, дата; нездобутий — пунктирна рамка, сірий шестикутник.
- **Карта проходження** (`.map`): grid 12 тем × 4 дії, знаки стану + бали; горизонтальний скрол
  у власному контейнері на вузьких екранах.
- **Кабінет**: сегментований перемикач представлень (Картки / Таблиця / Матриця ПРН),
  пошук, селекти модуля й Блума, чипи типів (`.fchip`, активний — чорнильний), рядок «Показано
  8 із 44». Таблиця: липкий заголовок, чекбокси лише в режимі вивантаження, вибраний рядок —
  `--surface-tint`, формати як маленькі бокси (PDF, DOCX, XML, SCORM), рівень Блума — крапка + слово.
  Матриця: перший стовпець липкий, покриття — заповнене коло / обведене / порожнє, підсумки в
  `tfoot`. Панель вивантаження (`.export-panel`): липка знизу, «Вибрано 7 матеріалів · 6,6 МБ»,
  чипи форматів, «Зняти вибір» і «Зібрати ZIP». Готові пакети — три рядки-картки; пам'ятка про
  статистику Moodle.
- **Поля** (`.input`, `.select`, `.cb`): 40 px, рамка `--line-strong`, фокус — синя рамка +
  кільце 3 px; чекбокс з `indeterminate`.
- **Футер**: логотип, опис дисципліни, дві колонки посилань, рядок ліцензій.

## Стани

Ховер — зміна рамки/тла за 140 ms, ніколи лише колір тексту. Фокус — кільце `--focus` 2 px з
offset 3 px на всьому. Disabled — opacity 0,45 + `cursor: not-allowed`. Порожні стани: «не
розпочато» — порожнє коло, у профілі — сірий пунктирний бейдж. Помилка відповіді — карміновий
фон + хрест + пояснення (не лише колір). Завантаження — у макетах не показано (Astro: скелетон
для банку питань). Вибір тексту — `--selection`.

## Доступність

WCAG 2.2 AA: усі текстові пари ≥4,5:1 (перевірено для обох тем), великі іконки ≥3:1. Skip-link,
`aria-current`, `aria-pressed` на перемикачах, `role="group"` на варіантах, `aria-live` на
вердикті й панелі вивантаження, `role="progressbar"` з межами, `<title>`/`<desc>` на схемах,
підписи до іконок-кнопок, `visually-hidden` лейбли полів. Клавіатура: усі дії — кнопки/посилання;
поповер закривається Esc. `prefers-reduced-motion` вимикає анімації. Мінімальна ціль 44 px.
Мова `lang="uk"`.

## Адаптив

Брейкпоінти 560 / 640 / 760 / 900–960 / 1024 / 1280 / 1360. На 390 px горизонтального скролу
немає (перевірено на всіх п'яти макетах); ширшими можуть бути лише таблиця кабінету і карта
проходження — у власних `overflow-x: auto` контейнерах. Двоколонкові макети (тема, тест) стають
одноколонковими ≤900–960; зміст теми на мобільному не показується (в Astro — розкривний список),
терміни — через підкреслені слова в тексті.

## Що лишено на реалізацію в Astro

- Мобільне меню (навігація, перемикач виду) — у макетах приховано без замінника.
- Динаміка: справжній банк питань, підрахунок XP (`ProgressStore`), імпорт/експорт коду з zod,
  фільтрація й сортування таблиці, збирання ZIP (fflate), Pagefind.
- Стани завантаження й помилок імпорту, порожній стан кабінету (нічого не знайдено).
- Глосарій-поповер — компонент без React, позиціонування через CSS anchor або Floating UI.
- Друкові стилі повноцінно (у макетах — мінімум).
- OG-зображення модулів, favicon (шестикутник у кольорі бренду).

## Файли

- `design/tokens.css` — токени (світла + темна).
- `design/fonts/` — Open Sans Variable woff2 (8 файлів) + `fonts.css` + `OFL.txt`.
- `design/assets/` — офіційний логотип (синій і білий PNG з альфою).
- `design/mockups/` — `base.css`, `index.html`, `topic.html`, `quiz.html`, `cabinet.html`,
  `profile.html`.
- `design/screens/` — скріншоти 1440 і 390 для кожного макета; для index і topic — обидві теми.

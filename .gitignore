# dependencies and builds
node_modules/
dist/
# generated by npm run build:downloads (PDF, DOCX, Moodle XML, ZIP, manifest.json)
public/downloads/
dist-e2e/
dist-export/
.astro/
coverage/
build/
out/

# Playwright / test artifacts (may contain page snapshots with sensitive data)
.playwright-mcp/
playwright-report/
test-results/
blob-report/

# local environment
.env
.env.*
!.env.example
.DS_Store
*.log

# private control banks must never live in this public repo
**/banks/control/
*.control.yaml

# Moodle docker runtime data
tools/moodle/.data/
tools/moodle/out/

# design review screenshots (local only)
design/screens/

# local Moodle credentials (generated from env/*.env.example)
tools/moodle/env/*.env

# materials supplied by the lecturer (not ours to publish)
docs/source/
